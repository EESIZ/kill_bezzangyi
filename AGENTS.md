# Kill_bezzangyi

## 목표와 현재 범위

unlazy의 완수·검증 구조를 바탕으로, 작업 판단 기준을 명확히 하고 불필요한 토큰 비용을 줄이는 프로젝트다. 현 단계는 프로젝트 초기화와 판단 기준 연구다. 정량 분류나 자동 최적화가 구현됐다고 설명하지 않는다.

프로젝트 표시명은 `Kill_bezzangyi`, 스킬 식별자와 호출명은 `kill-bezzangyi` / `$kill-bezzangyi`다. 원본 출처·라이선스·호환 식별자는 NOTICE.md를 참조한다.

## 유지할 여섯 단계

1. 적용 여부 판단
2. 요청 범위 확인
3. Solo / Orchestrated 실행 방식 선택
4. 완료 조건 정의
5. 필요한 경우 작업 분해
6. 병렬 실행 가능 여부 판단

사용자는 단계 자체를 바꾸기보다 각 단계의 판단 기준을 정의하려 한다. 이 구분은 프로젝트의 분석 틀이며, 원본 코드에 구현된 여섯 단계 상태 기계라는 뜻은 아니다. 현재 정량 임계값·가중치·복잡도 분류기는 없다.

## 다음 작업

mydocs/project-context.md와 mydocs/research/task-complexity.md를 참고한다. 다음 논의는 1단계 적용 여부부터 시작한다. 연구 결과, 설계 제안, 실제 구현을 명확히 구분한다. 사용자와 합의하기 전에 분해 깊이·모델 배분·조기 종료 정책을 임의로 도입하지 않는다.

## 개발 절차

사용자의 Hyper-Waterfall 원칙을 따른다. 구현 목표와 범위를 기록하고, 구현 전 3~6단계 계획을 작성해 승인받는다. 승인된 범위 안에서 구현·검증·피드백 반영을 반복하고 결과와 남은 문제를 기록한다. 아키텍처 변경은 사용자 판단을 거친다.

- 계획: `mydocs/plans/task_{N}.md`
- 단계 및 최종 보고: `mydocs/working/task_{N}_step{M}.md`, `task_{N}_final.md`
- 피드백: `mydocs/feedback/`
- 작업 목록: `mydocs/orders/yyyymmdd.md`

Node.js 16 이상, 외부 런타임 의존성 없음. 전체 검사는 `npm test`, 구조 검사는 `node tests/self-check.mjs`다. 문서·표시명 변경은 관련 구조 검사를 우선하고, 동작 변경에는 해당 회귀 검사와 필요한 전체 검사를 수행한다. Rust·SVG 등 다른 프로젝트의 검사를 이 저장소의 필수 검사로 취급하지 않는다.

MIT 원저작자 고지를 보존한다. 기존 설치본이나 전역 훅 변경은 프로젝트 파일 수정과 구분한다.
