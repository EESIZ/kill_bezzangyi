interface:
  display_name: "Kill_bezzangyi"
  short_description: "Finish substantial work against runnable gates"
  default_prompt: "Use $kill-bezzangyi to complete this substantial task against explicit, verifiable gates."
policy:
  allow_implicit_invocation: true
